# Server Configuration
server.port=8080

# Datasource Configuration
spring.datasource.url=jdbc:mysql://desktop.local:3306/jobportal?useSSL=false&serverTimezone=UTC
spring.datasource.username=jobportal
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.initialization-mode=always

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging Configuration
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
logging.file.path=/Users/robertocastro/dev/logs

# Spring Security (if applicable)
# spring.security.user.name=your_user
# spring.security.user.password=your_password

# Other Spring Configurations
spring.application.name=Job Portal
# spring.main.allow-bean-definition-overriding=true

# Custom Properties
# custom.property.example=exampleValue
